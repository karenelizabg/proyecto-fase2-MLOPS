## Qué cambia

<!-- Resume en pocas líneas qué hace este PR y por qué. -->

Cierra #

## Cómo lo verifiqué

<!-- Qué corriste y qué viste. Sé explícito con lo que NO pudiste probar. -->

## Checklist

- [ ] La rama es `p2-NN-descripcion` (o `tipo/descripcion`) y el título es `P2-NN: descripción` (o `tipo: descripción`). Ver [CONTRIBUTING.md](../CONTRIBUTING.md).
- [ ] Corrí en local lo mismo que el CI, o expliqué arriba por qué no.
- [ ] No subí secretos: `.env`, claves ni credenciales.
- [ ] Actualicé README o docs si cambia el comportamiento o la configuración.
- [ ] Si toco archivos compartidos (`docker-compose.yml`, `nginx.conf`, `pyproject.toml`), lo avisé en la descripción.
