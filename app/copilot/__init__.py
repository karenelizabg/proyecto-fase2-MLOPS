"""Tier 6 — Dataset Copilot (P2-52): cliente LLM + herramientas MCP de solo lectura.

`agent.py` es el bucle modelo <-> herramientas; `server.py` lo expone por HTTP
al frontend. La única lectura de la API key ocurre en `storage/settings.py`.
"""
